Run full test suite:
1. Run unit tests first — stop and fix if any fail
2. Run integration tests — stop and flag if any fail
3. Report a combined summary of both
4. Only confirm ready to push if all tests pass
