# Launch Aspire Dashboard

Windows-only command: ensures Docker Desktop is running, then starts the Aspire orchestration host and opens the dashboard in the browser.

Run the PowerShell script using the Bash tool:

```bash
powershell -ExecutionPolicy Bypass -File ./aspire-run.ps1
```

Wait for output and inform the user of any errors or the dashboard URL once launched.
